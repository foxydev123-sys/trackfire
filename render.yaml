# One-click Render deploy (Dashboard → New → Blueprint → pick this repo).
services:
  - type: web
    name: trackfire
    runtime: node
    region: frankfurt
    buildCommand: "echo no build step"
    startCommand: "node server/index.js"
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: "22"
