// Where the multiplayer server is.
// Leave empty when the Node server also serves these files (the default and
// simplest setup). If you host the website somewhere else (e.g. Netlify,
// GitHub Pages), put your game server's WebSocket address here, e.g.:
//   window.TRACKFIRE_SERVER = 'wss://my-trackfire.onrender.com/ws';
window.TRACKFIRE_SERVER = '';
